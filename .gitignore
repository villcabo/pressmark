# Build
/cli/internal/themes/packs/
/dist/
/main.js
/node_modules/

# Salida de pruebas
/testdata/out/
*.pdf
!/testdata/golden/*.pdf
# Fixtures, not output: the PDF tests read these, so a clean clone needs them.
!/testdata/pdf/*.pdf

# Entorno
.env
.DS_Store
