## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The problem, not the patch. If it fixes an issue, link it: Closes #N -->

## How it was verified

<!-- Commands run, output seen. "Tests pass" is not verification; say which. -->

- [ ] `bun run build` and `make build` succeed
- [ ] `make test` passes (Go and TypeScript)
- [ ] `bunx eslint plugin/src` reports no errors
- [ ] Behaviour change checked against a real PDF, if the renderer was touched

## Risks

<!-- What could break, and what is left undone. Say "none" only if true. -->
